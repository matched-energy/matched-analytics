[dependency-groups]
dev = [
    "mypy>=1.14.1",
]

[project]
name = "matched-analytics"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "Joe Kwiatkowski", email = "joe@matched.energy"},
]
dependencies = []
requires-python = "==3.13.*"
readme = "README.md"
license = {text = "MIT"}


[tool.pdm]
distribution = false

[tool.pdm.dev-dependencies]
dev = [
    "-e file:///${PROJECT_ROOT}/m_elexon",
    "-e file:///${PROJECT_ROOT}/m_mapper",
]
